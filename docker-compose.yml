version: "3.8"
services:
  watchmate:
    build: .
    ports:
      - 80:8000
    environment:
      ADMIN_NAME: admin
      ADMIN_EMAIL: admin@admin.com
      ADMIN_PASSWORD: 8420admin
      PROD: 1
      EMAIL_HOST_USER: $EMAIL_HOST_USER
      EMAIL_HOST_PASSWORD: $EMAIL_HOST_PASSWORD
    volumes:
      - ~/watchmate_db:/home/watchmate/database
